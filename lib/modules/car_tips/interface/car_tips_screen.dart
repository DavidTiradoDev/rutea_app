import 'package:flutter/material.dart';

class CarTipsScreen extends StatelessWidget {
  const CarTipsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Center(child: Text('Car Tips')));
  }
}
