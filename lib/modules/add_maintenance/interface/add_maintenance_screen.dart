import 'package:flutter/material.dart';

class AddMaintenanceScreen extends StatelessWidget {
  const AddMaintenanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Add Maintenance')),
    );
  }
}
